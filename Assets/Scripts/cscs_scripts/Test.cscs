class MyMessageBusCallbackClass
{
    V6 = 0;
    MyMessageBusCallbackClass(init)
    {
        V6 = init;
    }

    function ReceiveMessage()
    {
        DebugLog("Callback from Message Bus: " + V6);
        V6 = V6 + 1;
    }
}
MyMessageBusCallback = new MyMessageBusCallbackClass(42);
myGameApiProxy = CreateGameApiProxyObject();
myMessageBusProxy = myGameApiProxy.GetMessageBusProxy();

myMessageBusProxy.SubscribeToMessage("MyMessageBusCallback", "WelcomeMessage");